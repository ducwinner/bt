import { DataTable } from '@shopify/polaris';
import { useState } from 'react';

function TablePricingDetail({ data, queryTag, queryCollection }) {
  const [rows, setRows] = useState(data);

  return;
}

export default TablePricingDetail;
